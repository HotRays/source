#!/bin/sh

. /lib/functions.sh
board=`board_name`

case "$board" in
	"tempe,tempe-h6v2")
		# allow lan/wan input
		uci set firewall.@zone[0].input='ACCEPT'
		uci set firewall.@zone[1].input='ACCEPT'
		uci commit firewall
	;;
esac
